import React, { FC } from 'react';
import './SeatingConfig.scss';

interface SeatingConfigProps {}

const SeatingConfig: FC<SeatingConfigProps> = () => (
  <div className="SeatingConfig">
    SeatingConfig Component
  </div>
);

export default SeatingConfig;
